#!/bin/bash 
# set -x # debug

# Kill docker & binaries processes before update
# $HOME/DGLD_GuardianCommandConsole/Stop_Nodes.sh
# $HOME/DGLD_GuardianCommandConsole/Stop_Nodes.dgld

cd $HOME/DGLD_GuardianCommandConsole
git pull origin master
