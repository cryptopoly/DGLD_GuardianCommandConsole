#!/bin/bash 

# set -x # debug

cd $HOME/DGLD_GuardianCommandConsole

$HOME/DGLD_GuardianCommandConsole/NodeStart_DGLD.dgld

$HOME/DGLD_GuardianCommandConsole/NodeStart_CBT.dgld

# Needs to start separate due to block sync requirement (add condition here?)
# $HOME/DGLD_GuardianCommandConsole/NodeStart_GuardNode.dgld
